services:
  core-api:
    build:
      context: .
    environment:
      CORE_API_SERVER_PORT: ${CORE_API_SERVER_PORT:-8080}
    ports:
      - "${CORE_API_HOST_PORT:-8081}:${CORE_API_SERVER_PORT:-8080}"
