services:
  webapp:
    build:
      context: ../..
      dockerfile: services/webapp/Dockerfile
    cpu_shares: 1024
    restart: unless-stopped
    ports:
      - "${WEBAPP_HOST_PORT}:80"
      - "${MINIO_DOWNLOAD_HOST_PORT}:8080"
    depends_on:
      core-api:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s
