services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: batch_downloader
      MYSQL_USER: batch_downloader
      MYSQL_PASSWORD: batch_downloader
    ports:
      - "${MYSQL_HOST_PORT:-3306}:3306"
    volumes:
      - scrapper_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 20

  scrapper-api:
    build:
      context: .
    environment:
      SCRAPPER_DATABASE_URL: mysql+asyncmy://batch_downloader:batch_downloader@mysql:3306/batch_downloader
      SCRAPPER_URL_PROTECTION_SECRET: local-development-change-me
      SCRAPPER_SCHEDULER_TIMEZONE: Europe/Madrid
      SCRAPPER_SCRAPE_MAX_APPS: ${SCRAPPER_SCRAPE_MAX_APPS:-0}
    ports:
      - "${SCRAPPER_API_HOST_PORT:-8000}:8000"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 20

  scrapper-scheduler:
    build:
      context: .
    command: ["scheduler"]
    environment:
      SCRAPPER_DATABASE_URL: mysql+asyncmy://batch_downloader:batch_downloader@mysql:3306/batch_downloader
      SCRAPPER_URL_PROTECTION_SECRET: local-development-change-me
      SCRAPPER_SCHEDULER_TIMEZONE: Europe/Madrid
      SCRAPPER_SCRAPE_MAX_APPS: ${SCRAPPER_SCRAPE_MAX_APPS:-0}
    depends_on:
      mysql:
        condition: service_healthy

volumes:
  scrapper_mysql_data:
