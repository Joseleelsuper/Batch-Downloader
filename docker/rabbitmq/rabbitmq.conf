# Disable the deprecated Management API metrics collector. Prometheus metrics
# remain available through the rabbitmq_prometheus plugin.
management_agent.disable_metrics_collector = true
deprecated_features.permit.management_metrics_collection = false
